<input type="submit"
       value="Save"
       class="button-accept"
       <%= "hx-#{verb}=#{path}" %>
       hx-trigger="click, keyup[ctrlKey&&key=='s'] from:closest .screen"
       hx-target="closest .page-screens"
       hx-swap="outerHTML"
       hx-push-url="true">
